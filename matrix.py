import numpy as np

rows = 640  # Number of rows in the matrix
cols = 480  # Number of columns in the matrix

# Create an empty matrix using NumPy
matrix = np.empty((rows, cols))

# Print the matrix
print(matrix)

